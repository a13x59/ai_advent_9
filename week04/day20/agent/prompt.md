## 🔥 День 19. Композиция MCP-инструментов

Создайте несколько MCP-инструментов, например:

👉 search
👉 summarize
👉 saveToFile

Реализуйте пайплайн:

👉 первый инструмент получает данные
👉 второй — обрабатывает
👉 третий — сохраняет результат

Проверьте:

👉 автоматическое выполнение цепочки
👉 корректность передачи данных между инструментами

Результат:

Автоматический пайплайн из нескольких MCP-инструментов

Формат:

Видео + Код

## Промпт:
В директории тестовый проект для обучения основам AI.

В директории проект (подпапка "agent"), он представляет собой агента на python, который посылает запрос к API DeepSeek, механизм сжатия контекста, память агента (сессионная, рабочая, постоянная), профили, стэйт машина (FSM) с задачами, инварианты и его веб страница.

В папке "fortune-telling-mcp" - mcp сервис для доступа к API гадания по шару (Magic 8 Ball) и по книгам (Bibliomancy).

В папке "currency-mcp" - mcp сервис для получения информации о курсах валют.

Необходимо для задания сделать следующее:

=============
Создайте несколько MCP-инструментов, например:

👉 search
👉 summarize
👉 saveToFile

Реализуйте пайплайн:

👉 первый инструмент получает данные
👉 второй — обрабатывает
👉 третий — сохраняет результат

Проверьте:

👉 автоматическое выполнение цепочки
👉 корректность передачи данных между инструментами

Результат:
Автоматический пайплайн из нескольких MCP-инструментов

=============

чего не хватает в текущем проекте? предложи что добавить, но пока не трогай сам код

## Тестовый пример
Найди информацию про летающие юниты в Heroes 3 и сохрани краткую сводку в файл summary.txt






