## 🔥 День 10. Управление контекстом: разные стратегии (без summary)

Реализуйте в агенте 3 разных стратегии управления контекстом (минимум), и переключатель между ними:

👉 Стратегия 1: Sliding Window

- храните только последние N сообщений
- всё остальное отбрасывайте

👉 Стратегия 2: Sticky Facts / Key-Value Memory

- введите отдельный блок “facts” (ключ-значение), который хранит важные данные из диалога
(например: цель, ограничения, предпочтения, решения, договорённости)
    
- обновляйте facts после каждого сообщения пользователя
- в запрос отправляйте: facts + последние N сообщений

👉 Стратегия 3: Branching (ветки диалога)

- сохраните checkpoint в диалоге
- создайте 2 ветки от одного места
- продолжите диалог в каждой ветке независимо
- переключайтесь между ветками

Протестируйте на одном и том же сценарии (например: “собираем ТЗ 10–15 сообщений”):

- 👉 прогоните сценарий на каждой стратегии
- 👉 сравните ответы и поведение агента

Сравните:

- 👉 качество ответа
- 👉 стабильность (не теряет ли важные детали)
- 👉 расход токенов
- 👉 удобство для пользователя

Результат:

Агент с 3 стратегиями управления контекстом (Sliding Window / Facts / Branching) + сравнение результатов

Формат:

Видео + Код

## Промпт:
В директории проект, он представляет собой агента на python, который посылает запрос к API DeepSeek, механизм сжатия контекста и его веб страница.
Нужно расширить возможности агента - замени текущий механизм саммаризации на 3 стратегии, которые переключаются параметром запроса:

👉 Стратегия 1: Sliding Window

- храните только последние N сообщений
- всё остальное отбрасывайте

👉 Стратегия 2: Sticky Facts / Key-Value Memory

- введите отдельный блок “facts” (ключ-значение), который хранит важные данные из диалога
(например: цель, ограничения, предпочтения, решения, договорённости)
    
- обновляйте facts после каждого сообщения пользователя
- в запрос отправляйте: facts + последние N сообщений

👉 Стратегия 3: Branching (ветки диалога)

- сохраните checkpoint в диалоге
- создайте 2 ветки от одного места
- продолжите диалог в каждой ветке независимо
- переключайтесь между ветками

Важно! - полная история все также должна храниться в базе и восстанавливаться между выключениями агента.

Результат - агент, который работает с контекстом согласно выбранной стратегии.

Внести необходимые изменения в storage.py, добавь нужные части, удали ненужные.

Для наглядности - добавь в UI соответствующие элементы, адаптируй compression-banner, замени его на вывод сводки по контексту в зависимости от выбранной стратегии.
## Ответ DeepSeek Harness:
Что изменено
storage.py
 — переписано хранилище
Убраны всё, что относилось к саммаризации (session_summaries, summary, summarized_count, saved_tokens); таблица дропается при миграции.
Полная история по-прежнему хранится в SQLite и восстанавливается при старте. Новые таблицы:
conversations — метаданные сессии: current_branch, strategy, window_size;
branches — каждая ветка хранит свою полную историю + checkpoint;
session_facts — блок фактов (JSON ключ-значение).
Новые методы: create_session, create_branch, switch_branch, list_branches, load_branch, load_facts/save_facts, set_session_meta.
main.py
 — три стратегии + новые endpoint'ы
Параметры запроса: strategy (sliding_window | sticky_facts | branching) и window_size (N).
Sliding Window — в запрос уходит только conversation[-N:], остальное отбрасывается (полная история при этом хранится в БД).
Sticky Facts — отдельный блок фактов обновляется после каждого сообщения пользователя (реальный вызов LLM с парсингом JSON, либо детерминированный мок), в запрос идут facts (system) + последние N.
Branching — checkpoint + две ветки от одной точки, независимое продолжение и переключение. В ответ добавляется сводка context, заменившая прежнее compression.
Новые endpoint'ы: POST /agent/{id}/branch, POST /agent/{id}/switch, GET /agent/{id}/branches; GET /agent/{id} теперь отдаёт strategy, window_size, facts, current_branch, branches.
index.html
 — UI
Селектор стратегии, поле «Размер окна N», панель веток (кнопка «Checkpoint + 2 ветки» и выбор активной ветки).
compression-banner заменён на context-summary, который показывает сводку в зависимости от стратегии: окно и отброшенные сообщения / список фактов / активную ветку и список веток.
Убрана группа «Экономия» (счётчик сэкономленных токенов больше не актуален).
Проверка
Поднял сервер в временном venv и прогнал сценарии через curl: все три стратегии, создание веток, переключение, независимое продолжение в ветке B, восстановление из БД после «перезапуска» (новый процесс), DELETE и 404 — всё отработало корректно. Тестовые артефакты (.venv, agent_history.db) удалены, рабочие изменения — только три файла: storage.py, main.py, index.html.

Один нюанс для ручного запуска: у меня не было fastapi/uvicorn/requests в системном Python, поэтому проверку делал во временном venv. Если вы запускаете проект в своей среде, зависимости должны быть те же, что и раньше (новых библиотек я не добавлял).
